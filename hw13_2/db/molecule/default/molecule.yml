---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
platforms:
  - name: instance
    box: ubuntu/focal64
    memory: 512
    cpus: 2
provisioner:
  name: ansible
  lint:
    name: ansible-lint
  #verifier:
  #  name: ansible
